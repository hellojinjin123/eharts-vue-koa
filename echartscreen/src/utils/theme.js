import chalk from '../../public/theme/chalk.json'
// obj = JSON.parse(data) 将其转换成对象；
// 调用 echarts.registerTheme('chalk', obj) 注册主题；
// 使用 echarts.init(dom, 'chalk') 创建图表，第二个参数即为刚才注册的主题名字。

export default chalk